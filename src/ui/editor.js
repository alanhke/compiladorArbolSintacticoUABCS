/**
 * Punto de extensión para manejar el editor y eventos de UI.
 * TODO: Mover aquí los listeners y manipulación DOM desde main.js.
 */

export function getEditorCode(editorElement) {
  return editorElement.value;
}
